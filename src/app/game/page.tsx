import React from 'react';
import GameBoard from '../../components/GameBoard';

const Game: React.FC = () => {
    return (
      <div>
        <GameBoard />
      </div>
    );
  };

export default Game;